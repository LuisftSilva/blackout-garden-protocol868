# Run locally

```bash
cd web
python3 -m http.server 8080
```

Open:

```text
http://localhost:8080
```

For Android PWA install, the clean method is to serve this folder from HTTPS or use the Android wrapper APK project.
